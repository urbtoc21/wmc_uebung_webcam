export interface CameraID {
    id: string
}

// Camera interface definition
export interface Camera {
  id: string
  name: string
  location: string
  description: string
  resolution: string
  status: string
  thumbnail: string
  frames: string[]
}

