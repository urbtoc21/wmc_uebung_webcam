import {WebSocketServer as WSS, WebSocket} from "ws";
import {ClientWebSocket} from "./messageTypes";

export class CameraWebSocketServer {
    private wss: WSS;

    constructor(port: number) {

    }
}