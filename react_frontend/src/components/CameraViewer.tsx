import {useEffect, useState} from 'react'
import {type LiveImageMessage, type PlayCameraMessage} from "../common/messageTypes.ts";

export const CameraViewer = () => {
    return (
        <></>
    )
}

